```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save

    browser->>server: POST /new_note_spa
    server-->>browser: 201 Created

    Note right of browser: Browser updates UI without reloading
```